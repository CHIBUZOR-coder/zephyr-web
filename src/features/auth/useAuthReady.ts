import { useAuthStore } from "./auth.store";

export const useAuthReady = () => useAuthStore((s) => s.hydrated);















































































































































